MediaDespVeiculos/PortePequeno8 = 
VAR MEDIDA = [MediaDespVeiculos/PortePequeno]

VAR CONTAGEMESCRITORIOS = CALCULATE(COUNT(dimLocal[Local]), FILTER(dimLocal, dimLocal[PorteER] = "PEQUENO PORTE"))

RETURN DIVIDE(MEDIDA, CONTAGEMESCRITORIOS)